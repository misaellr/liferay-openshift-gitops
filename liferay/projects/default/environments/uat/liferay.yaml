# OpenShift UAT Environment Overlay
# Flat keys (chart deployed directly from OCI, not as subchart)

image:
  tag: "2025.q4.12"
replicaCount: 1
resources:
  limits:
    cpu: '2'
    memory: 8Gi
  requests:
    cpu: 500m
    memory: 2Gi
