# OpenShift PRD Environment Overlay
# Flat keys (chart deployed directly from OCI, not as subchart)

image:
  tag: 2025.q1.12-lts
replicaCount: 2
resources:
  limits:
    cpu: '4'
    memory: 12Gi
  requests:
    cpu: '2'
    memory: 4Gi
