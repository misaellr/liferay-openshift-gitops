# Base Liferay config (shared across all environments)
# Flat keys (chart deployed directly from OCI, not as subchart)

image:
  tag: 2025.q4.12
serviceAccount:
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::294825482201:role/misael-cne-irsa
